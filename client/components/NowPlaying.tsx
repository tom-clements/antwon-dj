import { FC } from "react";
import { Flex } from "components/layout/Flex";

interface Props {
    roomId: string;
}

export const NowPlaying: FC<Props> = props => {
    return (
        <div>NowPlaying</div>
    );
};
