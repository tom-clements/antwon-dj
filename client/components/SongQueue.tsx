import { FC } from "react";
import { Flex } from "components/layout/Flex";

interface Props {
    roomId: string;
}

export const SongQueue: FC<Props> = props => {
    return (
        <div>Song queue element</div>
    );
};
